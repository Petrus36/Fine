# Neon Postgres connection string (Prisma)
DATABASE_URL="postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/fine?sslmode=require"

# Auth secret for the admin panel — generate with: openssl rand -base64 32
AUTH_SECRET=""
